#include "ImageFilter.h"

void ImageFilter::Initialize(ComPtr<ID3D11Device>& device, ComPtr<ID3D11DeviceContext>& context, int width, int height)
{
	// 화면이 꽉차도록 사각형을 하나 그려준다.
}

void ImageFilter::UpdateConstantBuffers(ComPtr<ID3D11Device>& context)
{
}
